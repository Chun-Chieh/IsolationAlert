package com.example.maple.dashboardtest.model.survey;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SURVEY_SELECTED_ANSWER".
*/
public class SurveySelectedAnswerDao extends AbstractDao<SurveySelectedAnswer, Void> {

    public static final String TABLENAME = "SURVEY_SELECTED_ANSWER";

    /**
     * Properties of entity SurveySelectedAnswer.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property TimeStamp = new Property(0, Long.class, "timeStamp", false, "TIME_STAMP");
        public final static Property QuestionId = new Property(1, String.class, "questionId", false, "QUESTION_ID");
        public final static Property Answer = new Property(2, String.class, "answer", false, "ANSWER");
    }


    public SurveySelectedAnswerDao(DaoConfig config) {
        super(config);
    }
    
    public SurveySelectedAnswerDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SURVEY_SELECTED_ANSWER\" (" + //
                "\"TIME_STAMP\" INTEGER," + // 0: timeStamp
                "\"QUESTION_ID\" TEXT," + // 1: questionId
                "\"ANSWER\" TEXT);"); // 2: answer
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SURVEY_SELECTED_ANSWER\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, SurveySelectedAnswer entity) {
        stmt.clearBindings();
 
        Long timeStamp = entity.getTimeStamp();
        if (timeStamp != null) {
            stmt.bindLong(1, timeStamp);
        }
 
        String questionId = entity.getQuestionId();
        if (questionId != null) {
            stmt.bindString(2, questionId);
        }
 
        String answer = entity.getAnswer();
        if (answer != null) {
            stmt.bindString(3, answer);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, SurveySelectedAnswer entity) {
        stmt.clearBindings();
 
        Long timeStamp = entity.getTimeStamp();
        if (timeStamp != null) {
            stmt.bindLong(1, timeStamp);
        }
 
        String questionId = entity.getQuestionId();
        if (questionId != null) {
            stmt.bindString(2, questionId);
        }
 
        String answer = entity.getAnswer();
        if (answer != null) {
            stmt.bindString(3, answer);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public SurveySelectedAnswer readEntity(Cursor cursor, int offset) {
        SurveySelectedAnswer entity = new SurveySelectedAnswer( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // timeStamp
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // questionId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // answer
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, SurveySelectedAnswer entity, int offset) {
        entity.setTimeStamp(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setQuestionId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setAnswer(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(SurveySelectedAnswer entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(SurveySelectedAnswer entity) {
        return null;
    }

    @Override
    public boolean hasKey(SurveySelectedAnswer entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
